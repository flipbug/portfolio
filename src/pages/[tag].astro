---
export async function getStaticPaths() {
  const allTags = ["painting", "drawing"];

  return allTags.map((tag) => ({
    params: { tag },
  }));
}

const { tag } = Astro.params;

// Redirect to the new gallery page structure
return Astro.redirect(`/gallery/${tag}`);
---
